# 猫步互联 Pro · 项目说明与构建指南

局域网文件共享与跨设备通信工具。轻量桌面端（Tauri 2.0 原生架构，安装包仅约 25MB）+ 手机/浏览器端（Web/Android）共享同一套组件与服务。

## 🚀 运行

```bash
npm install        # 首次安装依赖
npm start          # 启动 Tauri 桌面端开发环境（或 npm run tauri:dev）
npm run server     # 仅以 Node 独立跑服务端（无窗口调试）
# 或直接双击根目录下「启动程序.bat」秒开 release 桌面客户端
```

手机连接：桌面端主页点「扫码连接」，或浏览器访问 `http://<电脑IP>:3000` / `http://landisk.local:3000`。

## 🏗️ 架构总览

```
src-tauri/              Tauri 2.0 桌面端主控（Rust 原生轻量宿主 + JobObject 进程管理）
src-tauri/binaries/     Node.js SEA 编译的独立单可执行服务端（lan-disk-server.exe）
gui.html                桌面端薄壳（标题栏 + 悬浮 Dock + 7 视图）
desktop/ipc.js          Tauri 原生 IPC 混合桥接 + 服务生命周期 + 运行日志
desktop/app.js          桌面端逻辑（Bento 主页 / 学习计划 / 设置七面板 / 传输抽屉）
server.js               Express 入口（静态托管 + 路由挂载 + 流量统计）
server/config.js        全局状态 / isSafePath / 数据目录 ~/.landisk
server/routes/          devices chat files upload download system tools history remote speedtest study
server/services/        mdns（landisk.local 广播） trash（回收站） history（传输记录） system-control.ps1
public/                 Web 端（index.html + app.js + manifest.json + sw.js）
shared/                 全端共享：设计系统(apple-theme) 图标库 UI库 播放器 组件库（含 study-plan, remote-control）
```

### 功能清单
- **文件**：浏览全盘/互传目录、分片上传（秒传/断点续传）、下载/打包、右键菜单（分享/重命名/删除/复制路径）、书签
- **学习计划**：课程自动排课、学习打卡连胜、大文件课件联动、实操任务监督
- **回收站**：删除先进回收站，可恢复/彻底清空，30 天自动清理（`~/.landisk/trash`）
- **分享**：文件生成限时链接 + 二维码（1 小时~7 天），设置页可管理/撤销
- **影音剧院**：选目录扫描媒体 → 海报墙（视频自动截帧）→ 沉浸式播放
- **跨端消息**：iMessage 风格实时聊天（SSE），支持图片/语音
- **便携遥控与屏幕镜像**：系统音量调节/一键静音、电源总控（锁屏/睡眠/重启/关机/防误触确认）、低延迟屏幕实时镜像与移动端触控交互映射
- **剪贴板互通**：手机 ⇄ 电脑双向文本与图像同步
- **工具**：进程监视/结束、Shell 控制台、涂鸦板、局域网极速测速
- **安全**：PIN 密码 + 扫码免密 Token、IP 黑白名单、试错限流、敏感操作本机限制

### 数据目录
运行数据统一存放在 `~/.landisk/`：`blocked_ips.json`、`device_aliases.json`、`history.json`、`trash.json`、`trash/`、`study_plans.json`、`study_records.json`。

## 📦 构建与打包

```bash
npm run build          # 一键构建：包含桌面前端 desktop-dist、Sidecar 服务端及 Tauri NSIS 安装包与便携版
npm run build:artifacts # 整理最终发布物至 dist_output 目录
```

打包产物位于 `src-tauri/target/release/bundle/nsis/` 与 `dist_output/`：
- **安装包体积**：约 **25.36 MB**（相较旧版瘦身超 87%）
- **便携版**：独立免安装单文件 `LanDisk-Pro-2.1.0-Portable.exe`
- **运行环境**：零依赖，用户无需安装 Node.js 或任何依赖库

## 🛠️ 技术栈规范

- **桌面端宿主**：Tauri 2.0 (Rust) + WebView2
- **服务端 Sidecar**：Node.js SEA (Single Executable Application) 单可执行文件
- **前端体系**：纯原生 ES6+ Vanilla JS + 苹果玻璃拟态设计系统，零打包开销
- **移动端与网页**：Capacitor 8 / PWA 响应式支持
