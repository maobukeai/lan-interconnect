# 局域网互联 Pro - 构建说明

## ✅ 构建成功！

可执行文件已生成在 `release/win-unpacked/` 目录中。

## 📦 构建产物

### 便携版（推荐）
- **位置**: `release/win-unpacked/局域网互联 Pro.exe`
- **大小**: 约 213 MB
- **特点**: 无需安装，双击即可运行

## 🚀 使用方法

### 方式 1：直接运行
进入 `release/win-unpacked/` 目录，双击 `局域网互联 Pro.exe`

### 方式 2：使用启动脚本
在项目根目录双击 `启动程序.bat`

### 方式 3：开发模式运行
```bash
npm start
```

## 🔧 重新构建

如果需要重新构建项目，请执行以下步骤：

### 快速构建（推荐）
```bash
# 清理并构建
rmdir /s /q dist release
npm run build
```

### 使用构建脚本
```bash
build-clean.bat
```

## ⚠️ 常见问题

### 1. 构建失败："file is being used by another process"
**原因**: 程序仍在后台运行或文件被杀毒软件扫描

**解决方法**:
1. 关闭所有运行的程序实例（包括系统托盘图标）
2. 在任务管理器中结束 `electron.exe` 或 `局域网互联 Pro.exe` 进程
3. 临时禁用 Windows Defender 实时保护
4. 重新运行构建命令

### 2. 图标错误："invalid icon file"
NSIS 安装器需要 `.ico` 格式图标。当前配置已改为只构建便携版，避免了此问题。

如需生成安装版，请：
1. 将 `icon.png` 转换为 `icon.ico` 格式
2. 修改 `package.json` 中的图标配置

### 3. 端口被占用
如果启动时提示端口被占用：
1. 关闭之前运行的实例
2. 在设置中更改服务端口号

## 📝 输出目录说明

```
release/
├── win-unpacked/           # 便携版主目录
│   ├── 局域网互联 Pro.exe   # 主程序入口
│   ├── resources/          # 应用资源
│   └── ...                 # Electron 运行时
└── builder-debug.yml       # 构建调试信息
```

## 🛠️ 开发说明

### 修改代码后
1. 重启程序查看效果
2. 按 F12 打开开发者工具调试
3. 需要分发给他人时重新构建

### 更改应用图标
1. 准备 256x256 的 PNG 图片
2. 替换 `icon.png`
3. 重新构建

### 更改应用名称
修改 `package.json` 中的 `productName` 字段，然后重新构建。

## 📊 构建配置

当前配置 (`package.json`):
- **目标格式**: 便携版 (Portable)
- **架构**: x64
- **输出目录**: release/
- **压缩方式**: ASAR

## 💡 提示

- 便携版适合个人使用和测试
- 如需分发给多人使用，建议制作安装版（需要 .ico 图标文件）
- 程序首次启动可能会稍慢（Windows SmartScreen 验证）

---

**构建时间**: 2026-03-31
**Electron 版本**: 41.0.3
**electron-builder 版本**: 25.1.8
