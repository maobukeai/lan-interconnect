# 猫步互联 Pro · 项目说明与构建指南

局域网文件共享与跨设备通信工具。桌面端（Electron）+ 手机/浏览器端（Web）共享同一套组件与服务。

## 🚀 运行

```bash
npm install        # 首次
npm start          # 桌面端（自动启动共享服务，默认端口 3000）
npm run server     # 仅以 Node 跑服务端（无窗口）
```

手机连接：桌面端主页点「扫码连接」，或浏览器访问 `http://<电脑IP>:3000` / `http://landisk.local:3000`。

## 🏗️ 架构总览

```
main.js                 Electron 主进程（窗口/托盘/IPC：启停服务、系统信息、定时关机）
gui.html                桌面端薄壳（标题栏 + 悬浮 Dock + 6 视图）
desktop/ipc.js          IPC 桥 + 服务生命周期 + 运行日志
desktop/app.js          桌面端逻辑（Bento 主页 / 设置七面板 / 传输抽屉）
server.js               Express 入口（静态托管 + 路由挂载 + 流量统计）
server/config.js        全局状态 / isSafePath / 数据目录 ~/.landisk
server/routes/          devices chat files upload download system tools history remote speedtest
server/services/        mdns（landisk.local 广播） trash（回收站） history（传输记录） system-control.ps1（Windows 系统/音量/屏幕控制）
public/                 Web 端（index.html + app.js + manifest.json + sw.js）
shared/                 两端共享：设计系统(apple-theme) 图标库 UI库 播放器 组件×11（含 remote-control）
```

### 功能清单
- **文件**：浏览全盘/互传目录、分片上传（秒传/断点续传）、下载/打包、右键菜单（分享/重命名/删除/复制路径）、书签
- **回收站**：删除先进回收站，可恢复/彻底清空，30 天自动清理（`~/.landisk/trash`）
- **分享**：文件生成限时链接 + 二维码（1 小时~7 天），设置页可管理/撤销
- **影音剧院**：选目录扫描媒体 → 海报墙（视频自动截帧）→ 沉浸式播放
- **跨端消息**：iMessage 风格实时聊天（SSE），支持图片/语音
- **便携遥控与屏幕镜像**：系统音量调节/一键静音、电源总控（锁屏/睡眠/重启/关机/防误触确认）、低延迟屏幕实时镜像与移动端触控交互映射
- **剪贴板互通**：手机 ⇄ 电脑双向文本
- **工具**：进程监视/结束、Shell 控制台、涂鸦板、局域网极速测速
- **安全**：PIN 密码 + 扫码免密 Token、IP 黑白名单、试错限流、敏感操作本机限制

### 数据目录
运行数据统一存放在 `~/.landisk/`：`blocked_ips.json`、`device_aliases.json`、`history.json`、`trash.json`、`trash/`、`window.json`。

## 📦 构建

```bash
npm run build      # Electron 便携版 → release/win-unpacked/
```

注意事项：
- 构建前关闭运行中的实例（含托盘图标）
- `public/` 与 `shared/` 已配置 asarUnpack（Express 需要真实文件路径）
- 仅构建 Windows x64 便携版；如需安装版需将 `icon.png` 转为 `.ico`

## 🛠️ 开发

- 前端零构建：经典 `<script>` 全局命名空间，改动即生效（刷新/Electron 重启）
- 主题：`shared/apple-theme.css`，深浅色由 `html[data-theme]` + 系统偏好决定
- 新图标：`shared/icons.js` 的 `ICONS` 表加一条 path 数组，用 `Icons.render('name', 20)`
- 新增桌面/网页共用功能：放 `shared/components/`，两端各自接线
- 排障：Electron 渲染进程 console 会转发到终端（warn/error 级别）

**Electron**: 41.x · **electron-builder**: 25.x · **Node**: ≥18.15（用到 fs.statfs）
